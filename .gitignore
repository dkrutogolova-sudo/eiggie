/node_modules
/.next
/out
/build
*.tsbuildinfo
next-env.d.ts
.DS_Store
.env*.local
.vercel

# Transcoded project video — heavy, regenerable from the raw drops via
# `node scripts/transcode.mjs`. Poster JPGs are committed so the index works.
/public/work/**/*.mp4
/public/work/**/*.webm

